<%= form_for @post do |f| %>
   <%= f.label :title %>
   <%= f.text_field :title %>

   <%= f.label :body %>
   <%= f.text_area :body %>

   <%= f.label :resolved %>
   <%= f.text_field :resolved %>

   <%= f.submit "Submit" %>
 <% end %>
